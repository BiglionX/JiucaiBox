# 韭菜学院 / JiucaiBox 用户中心需求文档

**版本**：V1.0  
**日期**：2026年8月  
**目标读者**：产品经理、前端开发、后端开发、UI/UX 设计师  
**关联文档**：《项目说明书》《技术文档》《前端设计说明书》《管理后台需求文档》

---

## 1. 文档概述

本需求文档定义“韭菜学院 / JiucaiBox”H5 平台中**用户中心**的功能范围、页面结构、交互逻辑、数据字段与接口设计。

用户中心是用户在平台上的个人空间，承载**身份管理、学习记录、测评历史、内容贡献、隐私设置**等核心功能。设计上需体现平台的**防割、温暖、匿名优先**原则，确保用户感到安全、可控、被尊重。

---

## 2. 用户角色与使用场景

### 2.1 用户角色
- **游客**：未登录用户，可浏览大部分内容，但无法提交测评、发布故事、点赞评论等。
- **普通用户**：已登录用户，可使用全部功能。
- **匿名用户**：已登录但选择匿名展示的用户，在社区互动中隐藏真实身份。

### 2.2 典型使用场景
1. 用户想查看自己的学习进度和已学课程。
2. 用户想回看自己提交过的测评报告。
3. 用户想管理自己发布的“韭菜的泪花”故事。
4. 用户想修改匿名设置或切换昵称。
5. 用户想查看自己的评论和抱抱记录。
6. 用户想清除缓存或查看平台规则。

---

## 3. 功能架构

用户中心包含以下功能模块：

| 模块 | 功能点 | 说明 |
|------|--------|------|
| 登录/注册 | 微信授权登录、手机号登录、游客态提示 | 支持快速登录，降低门槛 |
| 个人资料 | 头像、昵称、匿名设置、简介 | 昵称默认随机生成，可修改 |
| 学习记录 | 已学课程、学习进度、最近学习 | 记录视频观看行为 |
| 我的测评 | 测评报告列表、报告详情入口 | 保存用户提交的所有测评 |
| 我的故事 | 我发布的故事列表、审核状态 | 展示已发布/待审/被驳回故事 |
| 我的互动 | 我的评论、我的抱抱 | 记录用户社区互动 |
| 消息通知 | 系统通知、审核结果通知 | 轻量通知，不打扰 |
| 设置 | 清除缓存、大字模式、关于我们、免责声明、隐私政策 | 基础设置与合规入口 |
| 隐私与安全 | 匿名设置、账号注销 | 强调隐私保护 |

---

## 4. 详细功能需求

### 4.1 登录/注册

#### 4.1.1 登录方式
- **微信 OAuth 授权登录**（推荐）：调用微信开放平台，获取 unionid/openid，静默授权，无需用户输入。
- **手机号验证码登录**：输入手机号，获取短信验证码，登录/注册一体。
- **游客模式**：不登录可浏览，但触发需要登录的操作时，弹出登录引导。

#### 4.1.2 登录流程
1. 用户点击“登录”按钮或触发需登录操作。
2. 弹出登录面板，提供微信登录和手机号登录两个选项。
3. 选择微信：跳转微信授权页，回调后自动登录。
4. 选择手机号：输入手机号 → 获取验证码 → 输入验证码 → 登录成功。
5. 首次登录自动创建账号，昵称随机生成（如“韭菜A123”），头像使用默认占位图。

#### 4.1.3 登录状态保持
- 使用 JWT token 存储于 localStorage，有效期 30 天。
- 打开应用时自动校验 token，过期则要求重新登录。

#### 4.1.4 异常处理
- 微信授权失败：提示“微信登录失败，请重试或使用手机号登录”。
- 验证码错误/过期：提示重新获取。
- 登录接口超时：提示网络异常。

---

### 4.2 个人资料

#### 4.2.1 资料展示
- 头像：默认提供 8 款防割主题头像（盾牌、叶子、水滴等），用户可选或上传自定义头像。
- 昵称：默认随机生成，用户可修改，限制 2-12 个字符，不允许包含敏感词。
- 匿名标识：显示当前匿名状态（默认开启）。
- 简介：可选，一句话介绍，限制 50 字。

#### 4.2.2 编辑资料
- 点击“编辑”进入修改页，可修改头像、昵称、简介。
- 昵称修改后需通过敏感词过滤。
- 保存后更新全局状态（Pinia userStore）。

#### 4.2.3 匿名设置
- **默认开启，不可关闭**（这是平台核心隐私保护机制）。
- 用户可以**刷新随机昵称**，每次刷新生成新昵称。
- 社区内所有展示（故事、评论）均使用匿名昵称，不显示真实身份。

---

### 4.3 学习记录

#### 4.3.1 记录方式
- 用户点击视频链接跳转后返回，前端调用接口标记该视频为“已学”。
- 后台记录：用户ID、视频ID、观看时间。
- 支持重复学习，但进度只标记为已完成。

#### 4.3.2 展示内容
- **最近学习**：显示最近观看的 3 门课程及进度条。
- **我的课程**：以卡片列表展示所有已开始学习的课程，包含课程封面、标题、已学节数/总节数、继续学习按钮。
- **学习统计**：累计学习课程数、累计学习视频数、累计学习时长（估算）。

#### 4.3.3 交互
- 点击课程卡片进入课程详情。
- 点击“继续学习”跳转到上次学到的视频位置（通过记录最后观看视频）。
- 支持清空学习记录（在设置中提供入口）。

---

### 4.4 我的测评

#### 4.4.1 列表展示
- 显示用户提交的所有测评报告，按时间倒序。
- 每条显示：提交时间、来源链接标题/域名、风险等级标签（高/中/低）、状态（分析中/已完成/失败）。
- 支持分页加载。

#### 4.4.2 报告详情入口
- 点击列表项跳转到 `/analysis/result/:id`。
- 若报告状态为“失败”，显示失败原因，可重新提交。

#### 4.4.3 空状态
- 无测评记录时显示引导：“还没有测评记录，去测一测你感兴趣的课程吧”，附按钮跳转测评中心。

---

### 4.5 我的故事

#### 4.5.1 列表展示
- 显示用户发布的所有“韭菜的泪花”故事。
- 每条显示：标题、发布时间、状态（待审核/已发布/已驳回）、抱抱数、评论数。
- 点击进入故事详情（若已发布）；待审核/驳回可查看预览。

#### 4.5.2 审核状态说明
- **待审核**：显示“审核中，请耐心等待”。
- **已发布**：正常展示。
- **已驳回**：显示驳回原因，可编辑后重新提交。

#### 4.5.3 操作
- 删除故事：仅限用户自己删除已发布故事，删除后不可恢复。
- 编辑重新提交：仅限被驳回故事。

---

### 4.6 我的互动

#### 4.6.1 我的评论
- 列表展示用户发表过的所有评论，按时间倒序。
- 每条显示：评论内容、所属故事标题、时间。
- 点击可跳转到对应故事详情。
- 支持删除自己的评论。

#### 4.6.2 我的抱抱
- 列表展示用户给哪些故事点过“抱抱”。
- 每条显示：故事标题、抱抱时间。
- 点击可跳转到故事详情。

---

### 4.7 消息通知

#### 4.7.1 通知类型
- **系统通知**：平台公告、新课程上线、新电台发布等。
- **审核结果**：故事审核通过/驳回通知。
- **互动通知**（可选）：有人评论我的故事（为避免打扰，默认关闭，可在设置中开启）。

#### 4.7.2 展示方式
- 用户中心顶部显示铃铛图标，带未读红点。
- 点击进入通知列表，按时间倒序。
- 每条通知显示：标题、内容摘要、时间、已读/未读状态。

#### 4.7.3 已读处理
- 点击单条通知标记为已读。
- 支持“全部标记已读”。

#### 4.7.4 限制
- 通知保留最近 100 条，更早自动清理。

---

### 4.8 设置

| 设置项 | 说明 | 交互 |
|--------|------|------|
| **大字模式** | 开启后全局字体放大 20% | 开关按钮，立即生效 |
| **清除缓存** | 清除本地图片缓存、临时数据 | 点击后弹出确认，清除成功提示 |
| **清空学习记录** | 删除所有学习进度（不可恢复） | 二次确认，红色警示 |
| **通知设置** | 是否接收互动通知 | 开关按钮 |
| **关于我们** | 平台介绍、使命愿景 | 静态页面 |
| **免责声明** | 平台免责条款全文 | 静态页面 |
| **隐私政策** | 用户隐私保护说明 | 静态页面 |
| **用户协议** | 用户注册与服务协议 | 静态页面 |
| **账号注销** | 永久删除账号及所有数据 | 二次确认，需输入验证码，数据保留 7 天冷静期 |

---

### 4.9 隐私与安全

- **强制匿名**：所有社区互动默认匿名，不可关闭。
- **数据加密**：用户敏感信息加密存储。
- **权限透明**：展示平台收集的数据类型与用途（链接到隐私政策）。
- **账号注销**：提供注销入口，注销后所有个人数据删除，不可恢复。

---

## 5. 页面结构设计

### 5.1 个人中心主页 `/mine`

**布局**（从上到下）：
1. **用户信息头部**：头像、昵称、匿名标识、编辑按钮。
2. **数据概览**（可选）：已学课程数、测评次数、故事数。
3. **功能列表**（分组菜单）：
   - 学习记录
   - 我的测评
   - 我的故事
   - 我的互动
   - 消息通知
4. **设置**：设置入口、关于、免责声明、隐私政策。
5. **退出登录**：红色文字按钮，点击弹出确认。

**交互**：
- 未登录时，头部显示“点击登录”，功能列表点击均触发登录。
- 登录后显示完整内容。

### 5.2 编辑资料页 `/mine/edit`

- 表单：头像选择/上传、昵称输入、简介输入。
- 底部“保存”按钮。
- 保存成功后返回个人中心。

### 5.3 学习记录页 `/mine/learning`

- 顶部统计卡片。
- 最近学习列表。
- 我的课程列表。

### 5.4 我的测评列表 `/mine/analysis`

- 同测评中心的历史列表，但只显示当前用户的。

### 5.5 我的故事列表 `/mine/stories`

- 状态标签筛选（全部/待审核/已发布/已驳回）。
- 列表项展示。

### 5.6 我的互动页 `/mine/interactions`

- Tab 切换：我的评论 / 我的抱抱。

### 5.7 消息通知页 `/mine/notifications`

- 通知列表，带未读红点。

### 5.8 设置页 `/mine/settings`

- 分组设置项列表。

### 5.9 静态页面
- `/about`：关于我们
- `/disclaimer`：免责声明
- `/privacy`：隐私政策
- `/terms`：用户协议

---

## 6. 数据字段与接口

### 6.1 用户表字段（与后端一致）
```sql
users:
  id, openid, phone, nickname, avatar, bio, is_anonymous, created_at, last_active_at, status
```

### 6.2 主要接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/wechat` | 微信登录 |
| POST | `/api/auth/phone` | 手机号登录/注册 |
| GET | `/api/user/profile` | 获取当前用户资料 |
| PUT | `/api/user/profile` | 更新资料（昵称、头像、简介） |
| GET | `/api/user/learning` | 获取学习记录 |
| GET | `/api/user/analysis` | 获取我的测评列表 |
| GET | `/api/user/stories` | 获取我的故事列表 |
| GET | `/api/user/interactions` | 获取我的评论和抱抱 |
| GET | `/api/user/notifications` | 获取通知列表 |
| POST | `/api/user/notifications/read` | 标记通知已读 |
| POST | `/api/user/notifications/read-all` | 全部标记已读 |
| POST | `/api/user/clear-learning` | 清空学习记录 |
| POST | `/api/user/logout` | 退出登录 |
| DELETE | `/api/user/account` | 注销账号 |

所有需登录接口携带 JWT token。

---

## 7. 交互与视觉规范

- **风格**：与整体 H5 一致，温暖、克制、绿色主调。
- **图标**：使用线性图标，简洁。
- **空状态**：配合插画和引导文案。
- **加载**：列表页使用骨架屏。
- **错误**：toast 提示，不打断操作。
- **响应式**：移动端优先，最大宽度 640px。

---

## 8. 权限与状态控制

- **游客**：可进入个人中心查看静态内容，但登录后功能不可用。
- **普通用户**：所有功能可用。
- **被封禁用户**：禁止登录，或登录后只能浏览，禁止发布、评论、测评。

---

## 9. 验收标准

1. 用户可通过微信/手机号登录，登录后显示正确资料。
2. 昵称默认随机生成，可修改，且强制匿名在社区生效。
3. 学习记录准确记录已看视频，进度更新正确。
4. 我的测评列表与详情可正常查看。
5. 我的故事列表显示状态，被驳回可重新编辑提交。
6. 评论和抱抱记录可查询，删除操作生效。
7. 通知列表可读、可标记已读。
8. 设置项可正常使用，清除缓存、清空学习记录有二次确认。
9. 账号注销流程完整，有冷静期提示。
10. 所有页面在移动端显示正常，交互流畅。

---

## 10. 与整体系统关系

用户中心是前端“我的”页面的详细设计，与前端设计说明书中的 `/mine` 路由对应。它依赖后端用户系统、学习记录、测评、故事等模块的数据接口，同时是管理后台用户管理的对应端。

用户中心的设计需保证与全平台匿名机制、隐私保护政策一致，体现“防割学院”的信任感和安全性。

---

**文档结束**
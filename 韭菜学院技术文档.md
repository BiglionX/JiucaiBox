# 韭菜学院 / JiucaiBox 技术文档

**版本**：V1.0  
**日期**：2026年8月  
**文档状态**：初稿  
**目标读者**：开发团队、技术负责人、运维人员  

---

## 1. 文档概述

本文档描述“韭菜学院 / JiucaiBox”H5 平台的技术架构、功能实现、数据模型与接口设计。  
项目核心原则：**极低成本、快速上线、轻量运营、合规安全**。

平台以 H5 网站为中枢，视频内容托管于抖音、视频号等第三方平台，H5 仅存储视频标题、封面和跳转链接。通过调用大模型 API 实现 AI 生成课程内容、AI 测评分析与智能审核，降低人力成本。  

---

## 2. 系统架构

系统采用**前后端分离**架构，前端为响应式 H5 单页应用，后端提供 RESTful API，AI 能力通过第三方大模型 API 实现，文件存储使用云对象存储（仅存图片/音频等少量文件）。

```
[用户浏览器/移动端]
        |
        V
[CDN / 静态资源]  (H5 前端部署)
        |
        V
[负载均衡/Nginx]
        |
        V
[应用服务器]  (Node.js / Python)
   |        |        |
   |        |        +--> [AI 大模型 API] (DeepSeek/通义/文心)
   |        |
   |        +--> [MySQL/PostgreSQL]  (主数据库)
   |        |
   |        +--> [Redis]  (缓存/会话/队列)
   |        |
   |        +--> [对象存储 OSS]  (图片、音频、用户上传附件)
   |        |
   |        +--> [第三方平台]  (抖音、视频号 - 仅链接跳转)
```

**说明**：  
- 视频不存储于自有服务器，用户点击链接直接跳转抖音/视频号播放，节省带宽与存储成本。  
- 前端可部署于静态托管（如 Vercel、Netlify、腾讯云 COS 静态网站），减少服务器压力。  
- 后端使用云函数或轻量服务器（2核4G 即可支撑初期）。

---

## 3. 技术选型

| 层次 | 技术选择 | 理由 |
|------|----------|------|
| 前端 | Vue 3 + Vite + Vant UI | 轻量、移动端友好、生态成熟 |
| 后端 | Node.js (NestJS) 或 Python (FastAPI) | 开发效率高，适合轻量 API 服务 |
| 数据库 | MySQL 8 / PostgreSQL | 关系型数据，适合结构化内容与用户数据 |
| 缓存 | Redis | 存储会话、热点数据、限流 |
| 文件存储 | 腾讯云 COS / 阿里云 OSS | 低成本对象存储，用于图片、音频等 |
| AI 能力 | 大模型 API（DeepSeek、通义、文心） | 生成内容、测评分析、自动审核 |
| 部署 | Docker + 云服务器 / Serverless | 灵活部署，按需扩展 |
| 监控 | 自建简单日志 + 前端埋点 | 轻量，够用 |

---

## 4. 功能模块详细设计

### 4.1 用户系统
- 支持微信 OAuth 授权登录（可选），游客可浏览大部分内容，但测评提交、分享、收藏需登录。
- 用户字段：`id, openid/手机号, 昵称, 头像, 是否匿名, 注册时间, 最后活跃`。
- 匿名机制：用户可设置默认匿名，昵称系统随机生成（如“韭菜A123”）。

### 4.2 内容展示模块（课程/电台/体验营）
**核心特点**：所有视频内容在抖音、视频号发布，H5 仅存储视频元数据与链接。

**数据表结构示例**（简化）：

```sql
-- 视频内容表
CREATE TABLE `videos` (
  `id` INT AUTO_INCREMENT PRIMARY KEY,
  `type` ENUM('course', 'radio', 'experience', 'truth') NOT NULL COMMENT '类型：课程/电台/体验营/真相课',
  `module` VARCHAR(50) COMMENT '所属模块：财商/直播/加盟',
  `title` VARCHAR(255) NOT NULL,
  `cover_url` VARCHAR(255) COMMENT '封面图URL',
  `video_url` VARCHAR(500) NOT NULL COMMENT '抖音/视频号链接',
  `duration` INT COMMENT '时长(秒)',
  `description` TEXT,
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME ON UPDATE CURRENT_TIMESTAMP
);

-- 课程包/系列表
CREATE TABLE `courses` (
  `id` INT AUTO_INCREMENT PRIMARY KEY,
  `title` VARCHAR(255),
  `description` TEXT,
  `cover_url` VARCHAR(255),
  `is_free` BOOLEAN DEFAULT TRUE,
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 课程-视频关联
CREATE TABLE `course_video` (
  `course_id` INT,
  `video_id` INT,
  `order` INT,
  PRIMARY KEY (`course_id`, `video_id`)
);
```

**前端展示**：  
- 首页按模块展示课程入口。  
- 课程详情页列出视频列表，每个视频项显示封面、标题、时长，点击跳转 `video_url`（新窗口打开抖音/视频号）。  
- 完播后用户手动返回，触发学习记录 API。

### 4.3 反割韭菜测评模块

**流程**：  
1. 用户提交外部视频/课程链接或截图。  
2. 后端调用大模型 API 进行风险特征分析。  
3. 返回结构化风险报告（风险等级、风险点、建议）。  
4. 若用户选择“深度接洽流程”，则提供标准化避坑清单，用户按步骤反馈勾选项，系统综合评估。

**AI 提示词设计**：  
- 输入：用户提供的文本、链接标题、描述（用户可粘贴文案）。  
- 输出 JSON 格式：
```json
{
  "risk_level": "high|medium|low",
  "risk_points": [
    {"type": "收益承诺", "evidence": "出现'月入过万'", "count": 3},
    {"type": "紧迫性话术", "evidence": "出现'最后3天'"}
  ],
  "analysis": "该视频存在明显夸大宣传...",
  "recommendation": "建议谨慎对待，先学习真相课第X节..."
}
```

**接口设计**：  
- `POST /api/analysis`：提交测评，返回报告ID。  
- `GET /api/analysis/{id}`：获取报告详情。  
- `POST /api/analysis/{id}/deep`：提交深度接洽反馈，更新报告。

**数据库表**：  
- `analysis_reports`：记录报告ID、用户ID、原始链接、AI输出JSON、创建时间、状态。  
- `deep_feedback`：记录用户深度流程的每个步骤勾选结果。

### 4.4 韭菜体验营 / 财商学院 / 致富营（内容类似）
- 课程内容由 AI 生成，视频制作可先由 AI 生成文案、配音、简易动画，再发布到抖音/视频号。  
- H5 端展示视频列表，每节课后设置有**真相弹窗**（前端弹窗，内容可配置）。  
- **收益预期校准测试**：每章完成后弹出测试题，前端判分后展示真实数据参考。  
- 数据表：`tests` 记录测试题目和选项，`user_test_results` 记录用户答案与校准结果。

**真相弹窗实现**：  
- 弹窗内容存储在 `popups` 表，关联视频ID。  
- 前端播放完视频后（可通过链接跳转返回后触发，或用户点击“我已看完”按钮）请求 `/api/popup/{video_id}` 获取弹窗内容。

### 4.5 韭菜的泪花（UGC 社区）
- 用户提交结构化分享内容（表单填写）。  
- 系统自动对图片进行打码（手机号、身份证、银行卡等），使用第三方 OCR + 规则，或调用大模型识别并模糊处理。  
- 支持匿名发布，默认匿名。  
- 分享内容需人工或 AI 审核后展示。  
- 数据结构：
  - `stories`：主表，字段包括领域、金额、损失类型、描述、教训、是否匿名、状态（待审/发布/驳回）。  
  - `story_images`：图片表，存储打码后图片URL。  
  - `comments`：评论表，支持“抱抱”按钮（点赞）。  

### 4.6 韭菜电台（新闻预警）
- 每周人工精选新闻（也可 AI 辅助采集），后台录入。  
- 数据表：`radio_episodes`，字段含标题、新闻来源链接、速览、套路拆解、防割提醒、关联课程ID等。  
- 前端展示为图文卡片，视频版可另存视频链接（同课程模块）。  

### 4.7 AI 能力整合
- **内容生成**：调用大模型生成课程脚本、测试题、真相弹窗文案。  
- **测评分析**：大模型进行风险特征识别，输出结构化 JSON。  
- **自动审核**：对“韭菜的泪花”分享内容进行色情、暴力、广告等检测，以及隐私信息识别。  
- **自动打码**：使用 OCR 识别图片中的敏感信息，打码后存储。  
- **智能标签**：对用户提交的测评链接自动打标签，归入常见套路库。

---

## 5. 数据库设计（关键表）

除上述表外，还需：

```sql
-- 用户表
CREATE TABLE `users` (
  `id` INT AUTO_INCREMENT PRIMARY KEY,
  `openid` VARCHAR(100) UNIQUE,
  `nickname` VARCHAR(50),
  `avatar` VARCHAR(255),
  `is_anonymous` BOOLEAN DEFAULT FALSE,
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 测评报告表
CREATE TABLE `analysis_reports` (
  `id` INT AUTO_INCREMENT PRIMARY KEY,
  `user_id` INT,
  `source_url` VARCHAR(500),
  `source_type` ENUM('video','article','other'),
  `input_text` TEXT,
  `ai_result` JSON,
  `risk_level` ENUM('high','medium','low') NULL,
  `status` ENUM('pending','done','failed') DEFAULT 'pending',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `deep_feedback` JSON NULL,
  INDEX idx_user (user_id)
);

-- 真相弹窗表
CREATE TABLE `popups` (
  `id` INT AUTO_INCREMENT PRIMARY KEY,
  `video_id` INT,
  `content` TEXT,
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 测试题表
CREATE TABLE `quiz_questions` (
  `id` INT AUTO_INCREMENT PRIMARY KEY,
  `course_id` INT,
  `chapter` INT,
  `question` TEXT,
  `options` JSON,
  `correct_option` INT,
  `explanation` TEXT
);

-- 故事分享表
CREATE TABLE `stories` (
  `id` INT AUTO_INCREMENT PRIMARY KEY,
  `user_id` INT,
  `category` VARCHAR(50),
  `loss_amount` DECIMAL(10,2),
  `loss_type` VARCHAR(50),
  `content` TEXT,
  `lesson` TEXT,
  `is_anonymous` BOOLEAN DEFAULT TRUE,
  `status` ENUM('pending','approved','rejected') DEFAULT 'pending',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 电台表
CREATE TABLE `radio_episodes` (
  `id` INT AUTO_INCREMENT PRIMARY KEY,
  `title` VARCHAR(255),
  `source_url` VARCHAR(500),
  `summary` TEXT,
  `tricks` TEXT,
  `warning` TEXT,
  `related_course_id` INT,
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

## 6. API 接口设计（示例）

所有 API 使用 RESTful 风格，返回 JSON。

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | `/api/courses` | 获取课程列表 |
| GET | `/api/courses/:id` | 获取课程详情及视频列表 |
| POST | `/api/analysis` | 提交测评请求 |
| GET | `/api/analysis/:id` | 获取测评报告 |
| POST | `/api/analysis/:id/deep` | 提交深度接洽反馈 |
| GET | `/api/popup/:video_id` | 获取视频对应的真相弹窗 |
| POST | `/api/stories` | 提交韭菜的泪花分享 |
| GET | `/api/stories?category=&status=approved` | 获取已审核的故事列表 |
| POST | `/api/stories/:id/hug` | 给故事点赞（抱抱） |
| GET | `/api/radio` | 获取电台节目列表 |

**接口安全**：  
- 所有 POST 接口需登录态（JWT token）。  
- 提交内容接口增加频率限制（Redis 限流），防止刷量。  
- 用户输入内容需过敏感词过滤。

---

## 7. 安全与合规

### 7.1 内容安全
- 用户提交的测评链接、分享故事经 AI 内容审核（调用第三方内容安全 API 或大模型自带审核）确保不含违法违规内容。  
- 图片上传自动打码（手机号、身份证、银行卡）后再存储。  
- 弹窗和课程内容本身通过内部审核。

### 7.2 数据安全
- 用户敏感信息加密存储（如 openid 不直接明文，可哈希）。  
- 传输层 HTTPS 强制。  
- 数据库仅内网访问，使用参数化查询防 SQL 注入。

### 7.3 法律合规
- 所有测评报告附带免责声明：“本报告由 AI 生成，仅供教育参考，不构成事实认定或法律意见。”  
- 分享故事默认匿名，涉及机构名称做模糊化处理（如“某机构”“某老师”）。  
- 网站底部挂备案号，如有需要可申请 ICP 备案（国内服务器必须）。

---

## 8. 部署与运维

### 8.1 环境
- **前端**：静态托管（腾讯云 COS 静态网站 + CDN），或使用 Netlify/Vercel（国内访问需加速）。  
- **后端**：一台轻量应用服务器（2核4G，Ubuntu 22.04），Docker Compose 部署 Node.js + MySQL + Redis。  
- **AI 服务**：调用第三方 API，按量付费，无需自建模型。

### 8.2 部署流程
1. 代码推送至 Git 仓库（GitHub/Gitee）。  
2. CI/CD 工具（如 GitHub Actions）构建前端，上传至 COS。  
3. 后端构建 Docker 镜像，推送至私有仓库，服务器拉取并重启容器。  
4. 数据库迁移使用 Flyway 或手动 SQL。

### 8.3 监控与日志
- 后端日志使用 `winston` 或 `morgan` 输出 JSON 日志，收集到本地文件或简单日志服务。  
- 前端埋点统计（如使用百度统计/自建埋点 API）记录页面访问、按钮点击、测评提交。  
- 使用阿里云监控或 Prometheus + Grafana（可选）监控服务器资源。

---

## 9. 性能优化与成本控制

- **视频不占用服务器带宽**，托管于第三方平台，成本几乎为零。  
- **图片压缩**：用户上传图片自动压缩为 WebP 格式，减小存储和加载。  
- **AI 调用优化**：  
  - 批量生成课程脚本时，合理设置 temperature 减少重复调用。  
  - 测评分析使用缓存：同一链接若已有报告，直接返回缓存结果。  
- **数据库索引**：对高频查询字段（如 `category`, `status`, `created_at`）建立索引。  
- **CDN 加速**：静态资源走 CDN，减少服务器压力。

---

## 10. 未来扩展

- **小程序版**：将 H5 封装为微信小程序，方便用户使用。  
- **风险机构数据库**：积累足够案例后，建立公开查询接口，供其他平台调用。  
- **AI 模型微调**：积累标注数据后，微调专用模型，提高风险识别准确率。  
- **多语言支持**：国际版防割工具。

---

## 附：第三方平台对接说明

- **抖音/视频号**：  
  - 视频发布采用人工或半自动（抖音开放平台可申请上传接口，但需资质）。初期手动上传，获取链接后录入后台。  
  - H5 跳转使用普通 URL 跳转，无需使用 SDK。  
- **微信登录**：  
  - 使用微信开放平台“网站应用”获取 unionid/openid，需企业资质。  
  - 初期可简化为手机号验证码登录，降低成本。

---

**结语**：本技术文档旨在以最低成本实现韭菜学院的核心功能，并预留扩展空间。通过第三方视频托管、AI 能力、轻量架构，项目可在 3 个月内由 2-3 人团队完成开发并上线。
# 韭菜学院 / JiucaiBox 前端设计说明书

**版本**：V1.0  
**日期**：2026年8月  
**目标读者**：前端开发、UI/UX 设计师、产品经理  

---

## 1. 文档概述

本说明书定义“韭菜学院 / JiucaiBox”H5 前端的设计规范、页面结构、交互逻辑与视觉风格。  
项目核心原则：**移动优先、轻量快速、温暖克制、防割导向**。

前端采用 Vue 3 + Vite 构建，UI 组件库使用 Vant（移动端友好）。所有视频内容跳转至抖音/视频号，H5 本身不承载视频播放，仅提供入口、学习记录、测评交互与社区功能。

---

## 2. 设计原则

1. **防割优先**：所有页面设计必须传递“风险提示”和“真实认知”，避免任何夸大、焦虑诱导元素。  
2. **移动优先**：主要用户来自手机端，界面以单列布局为主，确保手指操作舒适。  
3. **轻量快速**：首屏加载控制在 2 秒内，静态资源压缩，不依赖重型库。  
4. **温暖而理性**：色彩柔和，文案克制，传递“我们理解你，但请冷静”的情绪。  
5. **透明与信任**：任何测评结果、课程介绍都明确标注来源、局限性和免责声明。  
6. **可访问性**：对比度达标，支持大字体，关键操作有明确反馈。

---

## 3. 用户体验流程（核心路径）

### 3.1 新用户认知路径
```
首页 → 免费真相课（引流） → 完成学习 → 弹窗引导测评 → 提交课程链接 → 获得风险报告 → 推荐体验营
```

### 3.2 被割用户求助路径
```
首页 → 反割测评 → 提交链接/反馈 → 风险报告 → 引导“韭菜的泪花”分享 → 获得支持与课程推荐
```

### 3.3 社区互动路径
```
韭菜的泪花列表 → 阅读故事 → 抱抱/评论 → 激发共鸣 → 查看相关课程或提交自己的故事
```

---

## 4. 信息架构与路由

### 4.1 底部导航（移动端 Tabbar）
共 5 个标签：

| Tab | 名称 | 对应路由 | 图标语义 |
|-----|------|----------|----------|
| 1 | 首页 | `/home` | 盾牌/叶子 |
| 2 | 测评 | `/analysis` | 放大镜 |
| 3 | 课程 | `/courses` | 播放/书 |
| 4 | 泪花 | `/stories` | 水滴/心 |
| 5 | 我的 | `/mine` | 人像 |

### 4.2 主要页面路由

| 路由 | 页面 | 说明 |
|------|------|------|
| `/home` | 首页 | 聚合入口、电台速报、推荐课程 |
| `/analysis` | 测评中心 | 提交测评、历史报告列表 |
| `/analysis/result/:id` | 测评报告详情 | 风险雷达图、风险点、建议 |
| `/analysis/deep/:id` | 深度接洽流程 | 分步勾选避坑清单 |
| `/courses` | 课程中心 | 各模块课程列表 |
| `/course/:id` | 课程详情 | 视频列表、章节、真相弹窗、测试 |
| `/stories` | 韭菜的泪花 | 故事列表（可按分类/损失筛选） |
| `/stories/new` | 提交故事 | 表单填写 |
| `/stories/:id` | 故事详情 | 全文、评论、抱抱 |
| `/radio` | 韭菜电台 | 期数列表 |
| `/radio/:id` | 电台详情 | 案例拆解、关联课程 |
| `/mine` | 个人中心 | 学习记录、我的测评、我的故事、匿名设置 |

---

## 5. 视觉设计规范

### 5.1 色彩体系
以“韭菜绿”为主色，传递安全、生长与希望；辅以警示橙用于风险提示。

| 用途 | 色值 | 说明 |
|------|------|------|
| 主色 | `#4CAF50`（浅绿） | 按钮、标签、强调 |
| 主色深 | `#2E7D32` | 文字链接、图标 |
| 背景色 | `#F7F8FA` | 页面背景 |
| 卡片背景 | `#FFFFFF` | 内容卡片 |
| 警示橙 | `#FF9800` | 中风险提示、注意 |
| 危险红 | `#F44336` | 高风险提示、紧急弹窗 |
| 文字主 | `#212121` | 标题 |
| 文字次 | `#757575` | 描述 |
| 边框 | `#E0E0E0` | 分割线、卡片描边 |

### 5.2 字体
- 系统默认字体栈：`-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "PingFang SC", "Microsoft YaHei", sans-serif`  
- 字号：  
  - 标题：18px / 16px（加粗）  
  - 正文：14px  
  - 辅助文字：12px  
  - 最小字号不低于 12px，保证可读性。

### 5.3 图标
- 使用 Vant 自带图标库 + 自定义 SVG 图标。  
- 关键图标：盾牌（防割）、放大镜（测评）、水滴（泪花）、播放（视频）、警报（风险）。  
- 图标风格线性、简洁，颜色随状态变化（如风险等级）。

### 5.4 间距与圆角
- 基础间距单位：4px，常用 8/12/16/24px。  
- 卡片圆角 8px，按钮圆角 20px（胶囊）。  
- 页面左右边距 16px。

---

## 6. 核心页面设计

### 6.1 首页 `/home`

**目标**：快速传达平台价值，提供各模块入口，展示最新预警。

**布局**（从上到下）：
1. **顶部通栏**：Logo + 标语“每一滴泪花，都是路标” + 匿名开关（可选）。
2. **紧急预警横幅**（如有高优先级新闻）：红底白字，显示“最新曝光：XX骗局”，点击进入韭菜电台详情。
3. **核心功能区**：4 个快捷入口宫格（2x2）：
   - 反割测评（放大镜图标，橙色）
   - 真相课（播放图标，绿色）
   - 韭菜体验营（盾牌图标，蓝色）
   - 韭菜电台（喇叭图标，红色）
4. **推荐课程**：横滑卡片，展示免费课程（如“直播行业真相课”），带“免费”标签。
5. **韭菜的泪花** 最新故事预览：显示 2 条真实分享（匿名昵称 + 标题 + 损失金额）。
6. **韭菜电台** 最新一期速报：标题 + 一句话摘要，点击进入详情。

**交互**：
- 所有入口点击跳转对应页面。
- 预警横幅点击跳转电台详情。
- 下拉刷新可更新数据。

### 6.2 测评中心 `/analysis`

**目标**：让用户快速提交链接，查看历史报告。

**布局**：
1. **顶部说明卡片**：简述测评原理，“AI 分析 + 人工经验，结果仅供参考”。
2. **提交框**：输入框（可粘贴链接） + 提交按钮。支持上传截图（图片识别文字）。
3. **历史报告列表**：卡片列表，每条显示：提交时间、来源域名/标题、风险等级（颜色标签）、状态（分析中/已完成）。
4. **引导**：如果无报告，显示引导文案“把你的课程链接发给我，帮你看看风险”。

**交互**：
- 提交后显示 loading，并跳转到报告详情页（若已有缓存则直接显示）。
- 错误状态：链接无效、AI 调用失败等给出友好提示，允许重试。

### 6.3 测评报告详情 `/analysis/result/:id`

**目标**：以可视化方式展示风险分析结果，提供行动建议。

**布局**：
1. **风险等级头部**：大号标签（高风险/中风险/低风险），颜色对应红/橙/绿。
2. **风险雷达图**（可选，简化版可为条形图）：展示几个维度的得分（收益承诺、焦虑话术、虚假案例、信息不透明、合规性）。
3. **风险点列表**：每条风险点包含：类型、证据（引用的文本片段）、出现次数。点击可展开详情。
4. **综合建议**：一段温和的文字建议，如“该视频存在明显收益承诺和紧迫性话术，建议谨慎对待。可先学习真相课第 3 节了解此类话术。”
5. **深度接洽入口**：按钮“我想进一步接触，帮我避坑”，点击进入深度流程。
6. **免责声明**：灰色小字固定底部。

**交互**：
- 雷达图/条形图可点击查看详细说明。
- 深度接洽按钮跳转 `/analysis/deep/:id`。
- 支持分享报告给好友（生成图片或链接）。

### 6.4 深度接洽流程 `/analysis/deep/:id`

**目标**：引导用户在与对方客服/销售沟通时，按步骤记录关键信号，最终得到综合风险判断。

**布局**：
- 顶部进度条（第 X 步，共 Y 步）。
- 每一步展示一个“避坑清单”问题，用户勾选“是/否/不确定”。
- 示例问题：
  - 对方是否承诺具体收益？
  - 是否出现“名额有限，今天必须定”？
  - 是否要求提供身份证、银行卡或贷款？
- 每一步下方有“下一步”按钮。
- 最后一步展示综合评估结果（基于勾选结果计算风险值），并提供红色警报（如勾选了贷款相关）。

**交互**：
- 必须勾选才能进入下一步。
- 出现高风险选项时，立即弹出“紧急提示”弹窗（见组件设计）。
- 最终结果可保存到我的测评记录。

### 6.5 课程中心 `/courses`

**目标**：展示所有免费课程模块，吸引用户学习。

**布局**：
- 顶部 Tab 分类：全部 / 直播 / 财商 / 加盟 / 体验营。
- 课程卡片列表：每个卡片包含封面图、标题、简介、标签（免费、已学完、共 X 节）、点击进入详情。
- 卡片底部显示学习进度（如已学 2/7 节）。

**交互**：
- 点击卡片进入课程详情。
- 支持搜索和筛选（按分类）。

### 6.6 课程详情 `/course/:id`

**目标**：提供视频列表与配套的防割练习（真相弹窗、测试）。

**布局**：
1. **课程信息头部**：标题、简介、学习人数、免费标识。
2. **视频列表**：每个视频项显示序号、标题、时长、封面缩略图。点击项跳转至视频链接（抖音/视频号），新窗口打开。
3. **真相弹窗**：跳转返回后（或用户点击“我已看完”），弹出弹窗，展示该视频对应的套路解析（内容由后端配置）。
4. **收益预期校准测试**：每完成一节/一章，出现测试题（选择题），提交后立即显示真实数据对比。
5. **学习记录**：自动记录用户已看视频（基于点击返回行为），进度条更新。

**交互**：
- 点击视频项跳转前，需登录（否则提示）。
- 真相弹窗为模态框，必须点击“我知道了”才能关闭。
- 测试题答完显示解析，并可重新测试。

### 6.7 韭菜的泪花 列表 `/stories`

**目标**：展示用户分享的被割经历，传递共情与警醒。

**布局**：
- 顶部筛选栏：分类（全部/直播/财商/加盟/其他）、损失金额范围。
- 故事卡片：匿名昵称、发布时间、标题（如“被某财商课割了2万”）、摘要（前 100 字）、分类标签、损失金额（格式化显示，如“损失 ¥20,000”）、抱抱数。
- 卡片点击进入详情。

**交互**：
- 下拉加载更多。
- 点击“抱抱”按钮 +1，并短暂动画。
- 支持分享故事（脱敏后）。

### 6.8 故事详情 `/stories/:id`

**布局**：
- 标题、匿名昵称、发布时间。
- 正文（纯文本，自动分段）。
- 图片（已打码）横滑展示。
- “我学到的教训”高亮引用块。
- 抱抱按钮 + 次数。
- 评论区（简洁，支持文字评论）。
- 底部推荐：“相似经历的人还看了”相关课程/测评。

**交互**：
- 评论区禁止嘲讽，有举报按钮。
- 点击相关课程跳转。

### 6.9 提交故事 `/stories/new`

**目标**：引导用户结构化分享自己的被割经历。

**表单字段**：
1. 我被割的领域（单选：直播培训/财商/加盟/其他）
2. 损失金额（数字输入，可选）
3. 损失类型（多选：金钱/时间/心理/家庭矛盾）
4. 事情经过（文本域）
5. 对方的套路（文本域）
6. 我现在最想对大家说的一句话（短文本）
7. 是否匿名（默认开启，不可关闭）
8. 图片上传（可选，自动打码）

**交互**：
- 表单验证：必须填写“经过”和“套路”。
- 提交后显示“感谢分享，审核通过后将展示”，并引导学习相关课程。
- 弹窗提示隐私保护说明。

### 6.10 韭菜电台 列表 `/radio`

**布局**：
- 列表卡片：期号、标题、发布时间、来源标签（官方通报/权威媒体）。
- 点击进入详情。

### 6.11 电台详情 `/radio/:id`

**布局**：
1. 标题 + 来源链接（可点击查看原文）。
2. 事件速览（段落）。
3. 套路拆解（列表，图标+文字）。
4. 防割提醒（高亮背景）。
5. 关联学习：推荐课程按钮。
6. 分享按钮。

### 6.12 个人中心 `/mine`

**布局**：
- 用户信息（昵称、头像），支持切换匿名昵称。
- 学习记录：已学课程数、最近学习。
- 我的测评：历史报告列表入口。
- 我的故事：我提交的故事状态。
- 设置：清除缓存、关于我们、免责声明、隐私政策。

---

## 7. 关键组件设计

### 7.1 风险雷达图（或条形图）
- 展示维度：收益承诺、焦虑话术、虚假案例、信息不透明、合规性（5 维）。  
- 使用 ECharts 或轻量 Canvas 绘制，移动端简化成条形图。  
- 颜色随风险等级变化：高风险红、中风险橙、低风险绿。  
- 点击维度显示说明文字。

### 7.2 真相弹窗
- 模态框，半透明黑色遮罩。  
- 内容：标题“注意！这可能是套路” + 解析文本 + “我知道了”按钮。  
- 弹出时机：用户看完视频返回后，或点击“我已看完”。  
- 样式：白底圆角卡片，顶部有警示图标。

### 7.3 收益预期校准测试
- 选择题组件，选项点击后立即高亮。  
- 提交后显示结果面板：用户预期 vs 真实数据，用对比条展示。  
- 附来源引用（如“根据中国基金业协会数据”）。  
- 按钮“重新评估”可重做。

### 7.4 故事卡片
- 卡片包含：匿名头像、昵称、标题、摘要、损失金额标签、抱抱数。  
- 损失金额用橙色/红色突出。  
- 点击卡片进入详情。

### 7.5 抱抱按钮
- 心形或拥抱图标，点击 +1，动画（放大-缩小）。  
- 需登录后操作。  
- 防刷：同一用户对同一故事只能抱一次（后端限制）。

### 7.6 匿名开关
- 在提交故事、评论时默认开启，显示“匿名发布”。  
- 用户不可关闭（强制匿名），但可编辑随机昵称。

### 7.7 紧急提示弹窗
- 当用户勾选高风险选项（如“对方要求贷款”）时触发。  
- 红色背景，白色文字，内容：“请立即停止支付，保留证据，拨打 12315 / 110”。  
- 弹窗包含“我知道了”按钮，不自动关闭。

### 7.8 风险等级标签
- 小圆角标签，文字“高风险”/“中风险”/“低风险”。  
- 颜色对应红/橙/绿。  
- 显示在报告头、历史列表、课程卡片等位置。

---

## 8. 交互与动效

- **过渡动画**：页面切换使用淡入淡出（200ms），避免复杂动画影响性能。  
- **加载状态**：使用骨架屏（列表页）或加载中图标（按钮）。  
- **反馈**：所有点击有轻微按压效果（CSS `:active`）。  
- **错误提示**：toast 提示，不打断用户操作。  
- **下拉刷新**：列表页支持。  
- **空状态**：使用插画 + 文案，如“还没有故事，来说出你的经历”。

---

## 9. 响应式与适配

- **移动端优先**：设计基准为 375px 宽度，最大内容宽度 640px，居中显示。  
- **桌面端**：当屏幕宽度 > 768px 时，页面内容居中，背景变灰，保持单列布局不拆分多列。  
- **字体适配**：使用 rem 单位，根字号根据屏幕宽度微调。  
- **安全区**：适配 iPhone 刘海屏，底部导航增加安全区 padding。

---

## 10. 性能与前端优化

- **路由懒加载**：非首屏页面按需加载。  
- **图片懒加载**：所有图片使用 `loading="lazy"`。  
- **静态资源压缩**：图片 WebP，JS/CSS 压缩，Tree Shaking。  
- **CDN**：静态资源部署到 CDN。  
- **第三方视频链接**：不加载任何视频文件，跳转外链即可，减少带宽。  
- **缓存**：使用浏览器缓存，接口数据设置合理 Cache-Control。

---

## 11. 可访问性与合规

- 所有图片添加 `alt` 文本。  
- 颜色对比度符合 WCAG AA（正常文字 ≥ 4.5:1）。  
- 表单控件有标签关联。  
- 提供“大字模式”选项（在个人中心，放大基础字号 20%）。  
- 测评报告页面包含“生成文字版”功能，方便视障用户。  
- 页脚包含免责声明、隐私政策入口。

---

## 12. 状态管理与数据流

使用 Pinia 管理全局状态：

| 状态 | 描述 |
|------|------|
| `userStore` | 用户登录态、匿名设置、昵称 |
| `analysisStore` | 当前测评报告、历史列表 |
| `courseStore` | 课程列表、学习进度 |
| `storyStore` | 故事列表、当前故事、抱抱状态 |

数据请求通过 Axios 封装，统一处理 token、错误提示、重试。

---

## 13. 错误与边界状态

- **网络错误**：显示“网络开小差，请稍后重试”和重试按钮。  
- **AI 分析失败**：提示“分析失败，可能链接无效或内容过短”，引导用户重试或提交截图。  
- **视频链接失效**：检测到抖音/视频号链接打不开，提示“视频已删除或审核中”。  
- **数据为空**：显示空状态插画和引导文案。  
- **未登录**：部分操作（提交故事、抱抱、深度流程）要求登录，弹窗提示微信登录。

---

## 结语

本说明书定义了韭菜学院 H5 前端的整体设计框架，旨在通过简洁、温暖、高效的用户体验，帮助普通人轻松获取防割知识和工具。前端开发应严格遵循本规范，确保项目快速落地并具备良好的可维护性。
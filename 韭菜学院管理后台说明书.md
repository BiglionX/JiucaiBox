# 韭菜学院 / JiucaiBox 管理后台需求文档

**版本**：V1.0  
**日期**：2026年8月  
**目标读者**：产品经理、后端开发、前端开发、运营人员  
**关联文档**：《项目说明书》《技术文档》《前端设计说明书》

---

## 1. 文档概述

本需求文档定义“韭菜学院 / JiucaiBox”管理后台（Admin）的功能范围、页面结构、操作流程与权限设计。

管理后台用于支撑平台内容运营、用户管理、测评服务、社区审核、数据监控等核心业务。后台本身不面向普通用户，需独立部署，支持 PC 浏览器访问。

**核心原则**：高效、安全、可追溯、权限分离。

---

## 2. 用户角色与权限

| 角色 | 权限范围 | 说明 |
|------|----------|------|
| **超级管理员** | 全部权限 | 系统配置、角色管理、所有模块增删改查、敏感操作 |
| **内容运营** | 课程管理、电台管理、弹窗配置、首页推荐 | 负责内容发布与更新 |
| **审核专员** | 故事审核、评论审核、测评报告复核 | 负责用户生成内容与AI结果的二次把关 |
| **客服/运营支持** | 查看用户、查看测评报告、处理举报 | 辅助用户支持 |
| **数据分析** | 查看统计报表、导出数据 | 只读权限 |

权限通过 RBAC 实现，支持自定义角色。

---

## 3. 后台功能模块总览

| 模块 | 主要功能 | 使用角色 |
|------|----------|----------|
| 登录与安全 | 账号密码登录、验证码、操作日志 | 所有 |
| 仪表盘 | 核心数据概览、待办事项、快速入口 | 所有 |
| 课程管理 | 课程包管理、视频链接管理、真相弹窗配置、测试题管理 | 内容运营 |
| 测评管理 | 测评报告列表、详情查看、深度接洽反馈查看、手动复核 | 审核专员 |
| 故事管理（泪花） | 故事列表、审核、打码复核、评论管理、抱抱统计 | 审核专员 |
| 电台管理 | 期数管理、新闻录入、套路拆解配置、关联课程设置 | 内容运营 |
| 用户管理 | 用户列表、详情、封禁/解封、学习记录查看 | 客服/运营 |
| 风险词库与标签 | 风险词条管理、常见套路标签、AI提示词配置 | 内容运营 |
| 系统配置 | 首页推荐位、轮播图、免责声明、紧急预警横幅 | 超级管理员 |
| 数据统计 | 课程完课率、测评提交量、用户增长、故事发布量、风险等级分布 | 数据分析 |

---

## 4. 详细功能需求

### 4.1 登录与安全

- **登录页**：账号密码 + 图形验证码，可选双因素认证（TOTP）。  
- **操作日志**：记录所有敏感操作（删除、审核、配置修改），包含操作人、时间、操作内容、IP。  
- **会话管理**：超时自动退出（30分钟无操作），支持手动退出。

### 4.2 仪表盘

**展示内容**：
- 今日关键数据：新增用户、测评提交数、故事待审数、课程完课数。  
- 风险等级分布（近7天）：高/中/低风险报告数量。  
- 待办事项：待审核故事、待复核测评、举报处理。  
- 快速入口：发布课程、录入电台、查看最新用户反馈。

**交互**：数据卡片可点击跳转至对应列表页。

### 4.3 课程管理

**课程包列表**：展示所有课程包（含课程标题、模块分类、视频数、状态、创建时间）。支持搜索、筛选、排序。

**新增/编辑课程包**：
- 填写：标题、简介、封面图、模块分类（直播/财商/加盟/体验营/真相课）、是否免费（固定为免费）、学习人数（可手动设置初始值）。  
- 关联视频列表：可添加、删除、排序视频。

**视频管理**（属于某个课程包）：
- 字段：视频标题、封面图、抖音/视频号链接、时长、描述、排序。  
- **重点**：视频链接需校验域名合法性（仅允许抖音、视频号等白名单域名），防止外链注入。  
- 支持批量导入（Excel/CSV）。

**真相弹窗配置**：
- 关联到具体视频，设置弹窗内容（富文本）。  
- 支持预览弹窗效果。  
- 可设置弹窗触发条件（如用户点击“我已看完”后）。

**测试题管理**：
- 关联课程包/章节。  
- 题型：单选。  
- 字段：题目、选项（JSON）、正确答案、解析文本。  
- 可设置每章结束强制弹出测试。

**学习进度管理**：查看每门课程的完课人数、平均进度，可导出。

### 4.4 测评管理

**测评报告列表**：
- 字段：报告ID、用户ID/昵称、提交时间、来源链接、风险等级、状态（AI分析中/已完成/失败/已复核）、处理人。  
- 支持按风险等级、时间、状态筛选。  
- 点击查看详情。

**测评报告详情**：
- 展示原始输入（链接/截图文字）。  
- 展示 AI 输出 JSON 的可视化视图（风险点、证据、雷达图数据）。  
- 展示用户深度接洽反馈（如有）：分步勾选结果、综合风险值。  
- **手动复核**：审核专员可修改风险等级、添加备注、标记为“已复核”。复核操作记录日志。

**深度接洽反馈查看**：
- 按报告 ID 查看用户每一步的勾选结果。  
- 显示最终风险结论与紧急警报触发情况。

**异常处理**：对 AI 分析失败的报告，可手动重新触发分析或标记无效。

### 4.5 故事管理（韭菜的泪花）

**故事列表**：
- 字段：故事ID、匿名昵称、领域分类、损失金额、发布时间、状态（待审核/已发布/已驳回）、抱抱数、评论数。  
- 支持按分类、状态、时间筛选。  
- 待审核故事置顶显示。

**故事详情与审核**：
- 展示全文、图片（打码前/打码后对比）。  
- 审核专员需确认：图片是否已自动打码（手机号、身份证、银行卡等）；若未完全打码，可手动标记区域重新打码或驳回要求修改。  
- 审核操作：通过、驳回（填写原因）、删除。  
- 通过后故事自动发布到前端。

**评论管理**：
- 查看故事下的所有评论。  
- 可删除违规评论、禁言用户。  
- 支持举报处理入口。

**抱抱统计**：查看每个故事的抱抱数，可追查用户抱抱记录（防刷）。

### 4.6 电台管理

**期数列表**：展示所有电台期数，含标题、来源、发布时间、关联课程。

**新增/编辑电台**：
- 字段：标题、来源链接（官方通报/权威媒体）、事件速览、套路拆解（列表，可添加多条）、防割提醒、关联课程（可选）。  
- 支持上传封面图。  
- 发布后前端展示。

**套路拆解配置**：每条套路需包含：套路名称、描述、对应风险标签（用于联动测评库）。

### 4.7 用户管理

**用户列表**：
- 字段：用户ID、昵称、头像、注册时间、最近活跃、学习课程数、测评次数、故事数、状态（正常/封禁）。  
- 支持搜索（昵称/ID）、筛选状态。

**用户详情**：
- 基本资料、学习记录（观看视频记录）、测评历史、故事发布历史、评论记录。  
- 操作：封禁/解封、重置昵称（强制匿名）、发送系统通知（可选）。

**匿名管理**：查看用户匿名设置情况，确保默认匿名不可关闭。

### 4.8 风险词库与标签

**风险词条管理**：
- 维护敏感词/风险词列表，如“月入过万”“躺赚”“最后3天”“保本保息”等。  
- 支持分类（收益承诺、焦虑话术、虚假案例等）。  
- 用于 AI 测评提示词调用，前端可展示风险词库。

**套路标签库**：
- 维护常见套路模板，如“快招公司”“精神传销”“杀猪盘”等。  
- 每个标签包含：名称、描述、识别要点、关联课程模块。  
- 用于测评报告的分类与推荐。

**AI 提示词配置**：
- 配置 AI 测评的系统提示词、输出 JSON 结构模板。  
- 支持版本管理，修改后新报告使用新提示词。

### 4.9 系统配置

- **首页推荐位**：设置首页显示的课程、故事、电台内容及排序。  
- **紧急预警横幅**：配置是否显示、文字内容、跳转链接（通常指向电台详情）。  
- **免责声明与隐私政策**：编辑全站统一声明文本。  
- **敏感词过滤**：配置用户输入内容（故事、评论）的敏感词库。  
- **图片自动打码规则**：设置打码敏感信息类型（手机号、身份证、银行卡、地址等）。  
- **第三方平台链接白名单**：管理允许的视频链接域名（抖音、视频号等）。

### 4.10 数据统计

- **核心指标**：用户增长趋势、测评提交量、故事发布量、课程完课率、平均学习时长。  
- **风险报告统计**：高/中/低风险占比、最常见的风险点类型。  
- **用户行为漏斗**：访问→提交测评→查看报告→深度流程→完成学习。  
- **内容效果**：各课程包的完课率、测试通过率、弹窗关闭率。  
- **导出功能**：支持按时间范围导出 CSV 报表。

---

## 5. 页面设计简要说明

- **整体风格**：简洁的企业后台风格，左侧导航菜单，顶部搜索栏+用户信息。  
- **响应式**：主要面向 PC 端（1280px 以上），不要求移动端适配。  
- **列表页**：使用表格展示，带分页、搜索、筛选、批量操作（如批量审核通过）。  
- **表单页**：分步或分组表单，支持富文本编辑。  
- **详情页**：分栏展示，左右布局（左侧内容，右侧操作日志/审核面板）。  
- **弹窗与提示**：使用组件库（如 Ant Design Vue）保持一致。  
- **权限控制**：根据角色动态显示菜单和按钮。

---

## 6. 非功能需求

- **性能**：列表页查询响应时间 < 500ms（数据量 10 万级）。  
- **安全**：后台必须独立域名，HTTPS，仅限内网或 IP 白名单访问（可选）。  
- **日志**：所有写操作记录日志，保留 6 个月。  
- **备份**：数据库每日自动备份，可恢复。  
- **可扩展性**：模块化设计，便于新增功能（如以后增加其他领域防割模块）。  
- **可用性**：99.5% 以上。

---

## 7. 接口需求（后台与前端共用）

后台所有操作通过 RESTful API 实现，部分接口与前端共享（如课程列表），部分仅后台可用（如审核操作）。

主要接口：
- `POST /admin/login` 登录  
- `GET /admin/dashboard` 仪表盘数据  
- `POST /admin/courses` 新建课程包  
- `PUT /admin/courses/:id` 更新课程包  
- `POST /admin/videos` 添加视频  
- `PUT /admin/videos/:id` 更新视频  
- `POST /admin/popups` 配置弹窗  
- `GET /admin/analysis?status=&risk_level=` 测评列表  
- `PUT /admin/analysis/:id/review` 复核测评  
- `GET /admin/stories?status=` 故事列表  
- `POST /admin/stories/:id/approve` 审核通过  
- `POST /admin/stories/:id/reject` 审核驳回  
- `DELETE /admin/stories/:id` 删除故事  
- `POST /admin/radio` 新建电台期数  
- `GET /admin/users` 用户列表  
- `PUT /admin/users/:id/ban` 封禁/解封  
- `GET /admin/stats/overview` 数据统计  

所有接口需带 JWT token，权限校验。

---

## 8. 验收标准

- 所有角色可正常登录，权限隔离正确。  
- 课程、视频、弹窗、测试题可正常创建、编辑、发布，前端实时生效。  
- 测评报告列表和详情可查，复核操作可保存并生效。  
- 故事审核流程完整：待审→通过/驳回，打码复核可操作。  
- 电台期数发布后前端可查看。  
- 数据统计页面展示准确，导出成功。  
- 操作日志记录完整。

---

## 9. 后续迭代建议

- 增加批量导入视频链接功能。  
- 对接第三方内容安全审核 API，自动审核故事和评论。  
- 支持 AI 辅助生成电台文案和套路拆解。  
- 增加机构黑名单/白名单管理，供测评参考。  
- 移动端后台简化版（用于紧急审核通知）。

---

**文档结束**